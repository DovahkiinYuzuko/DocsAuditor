# Docs Auditor 開発やることリスト

- [x] フェーズ1: プロジェクトの初期設定
  - [x] VS Code拡張機能（TypeScript）のひな形作成（`package.json`, `extension.ts`, `tsconfig.json`）
  - [x] LSPサーバー（Rust）の初期化と `Cargo.toml` の構成（`tower-lsp`, `tree-sitter` 等の依存追加）
- [x] フェーズ2: Rust側 HFSMの設計と実装
  - [x] `state/hfsm.rs` にて階層的な状態表現（`enum` 構成）と状態遷移関数を実装
  - [x] 状態遷移に応じたイベント処理ハンドラの実装
- [/] フェーズ3: tree-sitterによるAST解析の実装
  - [x] Markdownパーサーを用いた仕様書の「変数・関数宣言」の抽出
  - [x] Rustのtree-sitterパーサーを用いて、コード内の同名宣言を検索・抽出するロジックの実装
  - [ ] 引数の型・数、戻り値 of 型情報の抽出と検証ロジックの実装
- [ ] フェーズ4: 整合性検証・Diagnostics・Code Action
  - [ ] 差分検出時にエディタへ警告（Diagnostics）を返す仕組みの実装
  - [ ] 一致検出時に行番号を挿入するためのクイックフィックス（Code Action）の実装
- [ ] フェーズ5: 自動インジェクションとロック機構
  - [ ] `autoInjection` 設定に基づいた自動書き込み処理の実装
  - [ ] ファイルロックの実装（`locker.rs`）とHFSM `LockingWrite` 状態との統合
- [ ] フェーズ6: 自動レポート出力
  - [ ] 監査失敗時の `variables_functions_audit_report.md` 生成ロジックの実装
  - [ ] 全クリア時のレポート自動削除ロジックの実装
