# docs/variables'n'functions/[Rust]sample.md

## 概要
手動検証用のサンプル仕様書。このファイルを保存した際に監査が実行される。

## 監査対象シンボル
- fn add(a: i32, b: i32) -> i32
- fn subtract(a: u32, b: u32) -> i32 (L5-7)
- fn multiply(a: i32, b: i32) -> i32 (L12-14)
